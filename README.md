# ChatGPTIntegrationDJango
Need to install Django, openai and python-dotenv encironments:

pip install Django
pip install openai
pip install python-dotenv


To run the server on Windows:
python3 .\manage.py runserver